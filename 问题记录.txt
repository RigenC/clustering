1.数据库查询使用locate函数，用语查询频繁词集

2.分词后的词如果以http开头，应该去掉，是无意义的链接

会话抽取算法重新设计：
1.对消息流中的消息，只计算它与最新的K个会话的关系。最新的会话靠会话的最新事件来判断。会话窗口是滑动的，K的值是变动的，即变长滑动窗口机制
Tip:时间差作为判断依据，但不作为参数参与计算。因为相似度大小与时间存在关系，但不存在直接的连续关系，可以作为阈值判断标准
2.消息与会话的相似度通过消息与会话中的所有消息的相似度的最大值来表示
3.取消TextVector的ChildList属性，取消
4.这是单遍扫描算法，消息一旦划分所属会话之后将不再改变会话归属，继而将影响与之相关度最高的一系列后续消息。
5.加入用户信息，如果两个消息消息间隔小于<最小值>且是同一个人说的，则划分为同一会话
6.@符号加入考虑，但是@在分词的时候会被去，所以应该在transforFrom或者splitWord中处理，通过正则表达式匹配到@XXXX_格式的


3-10需要做的事情：splitword中添加response_to字段
sessionExtract中添加用户信息的判断
重新计算session